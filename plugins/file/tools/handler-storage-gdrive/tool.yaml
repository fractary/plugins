name: handler-storage-gdrive
type: tool
description: |
  Google Drive storage handler for fractary-file plugin

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/handler-storage-gdrive
  source_file: SKILL.md
  scripts_directory: scripts

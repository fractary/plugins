name: worktree-manager
type: tool
description: |
  Create and manage Git worktrees for parallel development workflows

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/worktree-manager
  source_file: SKILL.md
  scripts_directory: scripts

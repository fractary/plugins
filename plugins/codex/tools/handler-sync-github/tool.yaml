name: handler-sync-github
type: tool
description: |
  GitHub-specific sync mechanism - file copying, pattern matching, and safety checks

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/handler-sync-github
  source_file: SKILL.md
  scripts_directory: scripts
  workflow_directory: workflow

name: workflow-event-emitter
type: tool
description: |
  Emits structured workflow events for cross-project visibility and downstream consumption

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/workflow-event-emitter
  source_file: SKILL.md
  scripts_directory: scripts

name: state-manager
type: tool
description: |
  Manage issue lifecycle states (close, reopen, update state) via Fractary CLI

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/state-manager
  source_file: SKILL.md

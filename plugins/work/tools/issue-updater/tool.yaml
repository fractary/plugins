name: issue-updater
type: tool
description: |
  Update issue title and description via Fractary CLI

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/issue-updater
  source_file: SKILL.md

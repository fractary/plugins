name: comment-lister
type: tool
description: |
  List comments on issues with optional filtering via Fractary CLI

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/comment-lister
  source_file: SKILL.md

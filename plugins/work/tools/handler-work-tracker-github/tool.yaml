name: handler-work-tracker-github
type: tool
description: |
  "[DEPRECATED] GitHub Issues handler - Use Fractary CLI instead"

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/handler-work-tracker-github
  source_file: SKILL.md
  scripts_directory: scripts

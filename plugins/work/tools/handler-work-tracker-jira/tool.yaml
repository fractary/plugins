name: handler-work-tracker-jira
type: tool
description: |
  "[DEPRECATED] Jira Cloud handler - Use Fractary CLI instead"

input_schema:
  type: object
  properties:
    operation:
      type: string
    parameters:
      type: object

output_schema:
  type: object
  properties:
    status:
      type: string
      enum: [success, failure]
    result:
      type: object

implementation:
  type: skill-based
  skill_directory: plugins/repo/skills/handler-work-tracker-jira
  source_file: SKILL.md
  scripts_directory: scripts
